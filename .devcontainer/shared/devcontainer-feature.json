{
    "id": "shared",
    "version": "1.0.0",
    "name": "Wordclock shared editor configuration",
    "description": "Extensions, CMake settings and the toolchain sanity check shared by every devcontainer.json variant.",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools",
                "llvm-vs-code-extensions.vscode-clangd",
                "EditorConfig.EditorConfig",
                "eamodio.gitlens",
                "openai.chatgpt"
            ],
            "settings": {
                "cmake.configureOnOpen": true,
                "cmake.generator": "Ninja",
                "cmake.configureSettings": {
                    "PLATFORM": "simulator",
                    "CMAKE_EXPORT_COMPILE_COMMANDS": "ON"
                },
                "C_Cpp.intelliSenseEngine": "disabled",
                "clangd.arguments": [
                    "--compile-commands-dir=${workspaceFolder}/build"
                ]
            }
        }
    },
    "postCreateCommand": "wx-config --version && cmake --version"
}
