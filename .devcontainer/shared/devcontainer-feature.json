{
    "id": "shared",
    "version": "1.0.0",
    "name": "Wordclock shared editor configuration",
    "description": "Extensions, CMake settings, the toolchain sanity check and the PlatformIO package cache shared by every devcontainer.json variant.",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools",
                "llvm-vs-code-extensions.vscode-clangd",
                "EditorConfig.EditorConfig",
                "eamodio.gitlens",
                "openai.chatgpt"
            ],
            "settings": {
                "cmake.configureOnOpen": true,
                "cmake.generator": "Ninja",
                "cmake.configureSettings": {
                    "PLATFORM": "simulator",
                    "CMAKE_EXPORT_COMPILE_COMMANDS": "ON"
                },
                "C_Cpp.intelliSenseEngine": "disabled",
                "clangd.arguments": [
                    "--compile-commands-dir=${workspaceFolder}/build"
                ]
            }
        }
    },
    "containerEnv": {
        "GH_HOST": "github.com"
    },
    "postCreateCommand": "wx-config --version && cmake --version && pio --version",
    "mounts": [
        {
            "source": "wordclock-platformio",
            "target": "/home/vscode/.platformio",
            "type": "volume"
        },
        {
            "source": "wordclock-codex",
            "target": "/home/vscode/.codex",
            "type": "volume"
        },
        {
            "source": "wordclock-gh",
            "target": "/home/vscode/.config/gh",
            "type": "volume"
        }
    ],
    "postStartCommand": "platformio-trust-store"
}
